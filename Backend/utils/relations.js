// const db = require('./database')

// const batch = db.batch

// const team = db.teams

// const script = db.script

// team.hasMany(batch, { foreignKey: 'team_uuid' });

// batch.belongsTo(team, { foreignKey: "team_uuid" });

// team.hasMany(script, { foreignKey: 'team_uuid' });

// batch.hasMany(script, { foreignKey: "batch_uuid" })

// script.belongsTo(batch, {foreignKey: 'batch_uuid'});

// script.belongsTo(team, {foreignKey: 'team_uuid'});

